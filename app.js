/**
 * @file app.js
 */

(function(_global) {
    
class App {
    constructor() {
        this.cl = this.constructor.name;
    }
    async init() {
    }
}
    
if (false) {
} else {
    _global.App = App;
}
    
})( (this || 0).self || (typeof self !== 'undefined' ? self : global) );

